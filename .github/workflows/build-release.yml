name: Create Release
on:
  push:
    branches:
      - main
jobs:
  build-bot:
    name: Create Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install NPM dependencies
        run: npm install

      - name: Generate build
        run: num run build
